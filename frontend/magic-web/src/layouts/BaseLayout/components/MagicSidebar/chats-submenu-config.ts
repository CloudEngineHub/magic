/** Desktop sidebar ChatsSubMenu popup width in pixels; aligned with workspace/project popovers. */
export const DESKTOP_CHATS_SUBMENU_POPUP_WIDTH_PX = 240

/** When false, list rows hide timeLabel but keep mapping/render code for easy re-enable. */
export const DESKTOP_CHATS_SUBMENU_SHOW_TIME_LABEL = false

/** List-only scroll viewport height; header and search stay fixed above this area. */
export const DESKTOP_CHATS_SUBMENU_LIST_MAX_HEIGHT_PX = 400
