/** Topic-model cache id for mobile default audio recording preferences */
export const DEFAULT_AUDIO_SETTING_TOPIC_ID = "default_audio"

/** Default UI values when API extra fields are absent */
export const DEFAULT_RECORDING_SETTINGS = {
	transcription_enabled: true,
	auto_summary_enabled: true,
} as const
