import { logger } from "@/utils/log"

/** Host-side logger for HTML micro-app preview (manifest, permissions, grants). */
export const htmlMicroAppPreviewLogger = logger.createLogger("HtmlMicroAppPreview")
