/** Shared outer shell styles for PC audio recordings list and detail pages. */
export const AUDIO_RECORDINGS_PAGE_SHELL_CLASS =
	"flex h-full min-h-0 w-full min-w-0 flex-col overflow-hidden rounded-xl border border-border bg-background shadow-xs"

/** Share detail keeps the same white background as the owner page but must not add an extra card chrome on top of /share. */
export const AUDIO_RECORDINGS_SHARE_PAGE_SHELL_CLASS =
	"flex h-full min-h-0 w-full min-w-0 flex-col overflow-hidden bg-background"
